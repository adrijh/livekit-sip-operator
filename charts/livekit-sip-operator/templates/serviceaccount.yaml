apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "livekit-sip-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "livekit-sip-operator.labels" . | nindent 4 }}
