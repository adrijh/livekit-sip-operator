---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: egressconfigs.sip.livekit-sip.io
spec:
  group: sip.livekit-sip.io
  names:
    kind: EgressConfig
    listKind: EgressConfigList
    plural: egressconfigs
    singular: egressconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EgressConfig is the Schema for the egressconfigs API.
          It defines a reusable storage backend that can be referenced by SIPDispatchRule room egress configurations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              EgressConfigSpec defines a reusable egress storage backend configuration.
              Exactly one storage backend must be specified.
            properties:
              aliOSS:
                description: Alibaba Cloud OSS configuration.
                properties:
                  accessKeyRef:
                    description: 'Reference to a Secret and key for the access key.
                      Default key: "access_key".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                  bucket:
                    description: OSS bucket name.
                    type: string
                  endpoint:
                    description: Custom OSS endpoint.
                    type: string
                  region:
                    description: OSS region.
                    type: string
                  secretKeyRef:
                    description: 'Reference to a Secret and key for the secret key.
                      Default key: "secret".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - accessKeyRef
                - bucket
                - region
                - secretKeyRef
                type: object
              azure:
                description: Azure Blob Storage configuration.
                properties:
                  accountKeyRef:
                    description: 'Reference to a Secret and key for the account key.
                      Default key: "account_key".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                  accountNameRef:
                    description: 'Reference to a Secret and key for the account name.
                      Default key: "account_name".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                  containerName:
                    description: Azure storage container name.
                    type: string
                required:
                - accountKeyRef
                - accountNameRef
                - containerName
                type: object
              gcp:
                description: Google Cloud Storage configuration.
                properties:
                  bucket:
                    description: GCS bucket name.
                    type: string
                  credentialsRef:
                    description: 'Reference to a Secret and key for the service account
                      JSON credentials. Default key: "credentials".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - bucket
                - credentialsRef
                type: object
              s3:
                description: S3-compatible storage configuration.
                properties:
                  accessKeyRef:
                    description: 'Reference to a Secret and key for the access key.
                      Default key: "access_key".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                  bucket:
                    description: S3 bucket name.
                    type: string
                  endpoint:
                    description: Custom S3 endpoint (for MinIO, etc.).
                    type: string
                  forcePathStyle:
                    description: Force path-style URLs (required for MinIO).
                    type: boolean
                  region:
                    description: AWS region.
                    type: string
                  secretKeyRef:
                    description: 'Reference to a Secret and key for the secret key.
                      Default key: "secret".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                  sessionTokenRef:
                    description: 'Reference to a Secret and key for an optional session
                      token. Default key: "session_token".'
                    properties:
                      key:
                        description: Key within the Secret. If empty, the default
                          key for the field is used.
                        type: string
                      name:
                        description: Name of the Secret in the same namespace.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - accessKeyRef
                - bucket
                - region
                - secretKeyRef
                type: object
            type: object
          status:
            description: EgressConfigStatus defines the observed state of EgressConfig.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
