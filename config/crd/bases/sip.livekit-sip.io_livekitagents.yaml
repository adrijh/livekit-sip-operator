---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: livekitagents.sip.livekit-sip.io
spec:
  group: sip.livekit-sip.io
  names:
    kind: LivekitAgent
    listKind: LivekitAgentList
    plural: livekitagents
    singular: livekitagent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.agentName
      name: Agent
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.replicas
      name: Replicas
      type: string
    - jsonPath: .status.availableReplicas
      name: Available
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LivekitAgent is the Schema for the livekitagents API.
          It represents a LiveKit agent, providing a discoverable registry entry
          with optional health tracking via a referenced Kubernetes workload.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LivekitAgentSpec defines the desired state of LivekitAgent.
              A LivekitAgent represents an agent registered with a LiveKit server,
              providing a discoverable registry entry and optional health tracking
              via a referenced Kubernetes workload.
            properties:
              agentName:
                description: Name of the agent as registered in LiveKit.
                type: string
              deploymentRef:
                description: |-
                  Reference to the Kubernetes Deployment running the agent.
                  When set, the controller tracks the Deployment's health and
                  reflects replica status.
                properties:
                  name:
                    description: Name of the Deployment resource.
                    type: string
                required:
                - name
                type: object
              metadata:
                additionalProperties:
                  type: string
                description: |-
                  Arbitrary metadata about the agent (e.g. language, capabilities).
                  Useful for backend discovery and filtering.
                type: object
            required:
            - agentName
            type: object
          status:
            description: LivekitAgentStatus defines the observed state of LivekitAgent.
            properties:
              availableReplicas:
                description: Number of ready replicas from the referenced workload.
                format: int32
                type: integer
              conditions:
                description: Standard conditions for the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncedAt:
                description: Last time the resource was successfully reconciled.
                format: date-time
                type: string
              message:
                description: Human-readable message about the current state.
                type: string
              observedGeneration:
                description: The generation last observed by the controller.
                format: int64
                type: integer
              ready:
                description: |-
                  Whether the agent workload is healthy (all replicas ready).
                  Always true if no workloadRef is set.
                type: boolean
              replicas:
                description: Total desired replicas from the referenced workload.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
