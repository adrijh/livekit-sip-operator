---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: sipdispatchrules.sip.livekit-sip.io
spec:
  group: sip.livekit-sip.io
  names:
    kind: SIPDispatchRule
    listKind: SIPDispatchRuleList
    plural: sipdispatchrules
    singular: sipdispatchrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.dispatchRuleID
      name: Rule ID
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SIPDispatchRule is the Schema for the sipdispatchrules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SIPDispatchRuleSpec defines the desired state of a SIP dispatch
              rule.
            properties:
              attributes:
                additionalProperties:
                  type: string
                description: Attributes to set on SIP participants.
                type: object
              callee:
                description: Configuration for callee dispatch (type=callee).
                properties:
                  pin:
                    description: Optional PIN required.
                    type: string
                  randomize:
                    description: If true, append a random suffix to the room name.
                    type: boolean
                  roomPrefix:
                    description: Prefix for generated room names.
                    type: string
                required:
                - roomPrefix
                type: object
              direct:
                description: Configuration for direct dispatch (type=direct).
                properties:
                  pin:
                    description: Optional PIN required to enter the room.
                    type: string
                  roomName:
                    description: Name of the room to route calls to.
                    type: string
                required:
                - roomName
                type: object
              hidePhoneNumber:
                description: Hide the caller's phone number from participants.
                type: boolean
              inboundNumbers:
                description: Phone numbers that this rule accepts calls from.
                items:
                  type: string
                type: array
              individual:
                description: Configuration for individual dispatch (type=individual).
                properties:
                  noRandomness:
                    description: If true, don't append a random suffix to the room
                      name.
                    type: boolean
                  pin:
                    description: Optional PIN required.
                    type: string
                  roomPrefix:
                    description: Prefix for generated room names.
                    type: string
                required:
                - roomPrefix
                type: object
              krispEnabled:
                description: Enable Krisp noise cancellation.
                type: boolean
              livekitRef:
                description: 'Reference to a Secret containing LiveKit server credentials
                  (keys: url, api-key, api-secret).'
                properties:
                  port:
                    default: 7880
                    description: Port of the LiveKit HTTP API on the Service.
                    format: int32
                    type: integer
                  secretName:
                    description: 'Name of the Secret containing LiveKit API credentials
                      (keys: LIVEKIT_API_KEY, LIVEKIT_API_SECRET).'
                    type: string
                  service:
                    description: |-
                      Name of the LiveKit Kubernetes Service (in the same namespace).
                      The operator connects via http://<service>:<port>.
                    type: string
                required:
                - secretName
                - service
                type: object
              mediaEncryption:
                description: Media encryption mode.
                enum:
                - disable
                - allow
                - require
                type: string
              metadata:
                description: User-defined metadata.
                type: string
              name:
                description: Name of the dispatch rule.
                type: string
              numbers:
                description: Phone numbers that this rule accepts calls to.
                items:
                  type: string
                type: array
              roomPreset:
                description: Room preset (cloud-only).
                type: string
              trunkIDs:
                description: Trunk IDs that are accepted for this rule. If empty,
                  all trunks match.
                items:
                  type: string
                type: array
              type:
                description: Type of dispatch rule.
                enum:
                - direct
                - individual
                - callee
                type: string
            required:
            - livekitRef
            - name
            - type
            type: object
          status:
            description: SIPDispatchRuleStatus defines the observed state of a SIP
              dispatch rule.
            properties:
              conditions:
                description: Standard conditions for the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dispatchRuleID:
                description: The LiveKit dispatch rule ID returned after creation.
                type: string
              lastSyncedAt:
                description: Last time the resource was successfully reconciled.
                format: date-time
                type: string
              message:
                description: Human-readable message about the current state.
                type: string
              observedGeneration:
                description: The generation last observed by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
